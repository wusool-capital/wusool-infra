"""Persistence for `match_scores` — the only match-related table that exists.

No `match_runs`/`matches`/`match_evidence` tables in the real schema (see
`models.py`'s module docstring and the Phase 2 plan's schema-gap note).
"""
