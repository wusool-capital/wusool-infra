# ddl-commands

Slack bot for database DDL commands (schema changes against `wusool_crm`).
Not yet built.

Expected shape, similar to [`../matching-engine`](../matching-engine): Python
FastAPI + Slack Bolt, its own Postgres access, its own Secrets Manager
secret. No code, Dockerfile, or dependencies exist yet — this is a
placeholder marking the intended location.
