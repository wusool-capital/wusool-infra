"""Domain logic used by two or more tools — the ledger vocabulary, dedup,
prompts, sector mapping. Nothing single-tool belongs here; if only one tool
imports a file, it belongs in that tool's own domain/ subpackage instead.
"""
