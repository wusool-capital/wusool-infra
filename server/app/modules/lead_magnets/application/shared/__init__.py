"""Application logic every tool goes through — the write contract, per-tool
dispatch, the sweeper, and the Protocol ports. Nothing single-tool belongs
here; a tool's own AI orchestration lives in that tool's application/
subpackage instead.
"""
